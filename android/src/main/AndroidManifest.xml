<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required on Android 13+ to receive a non-zero advertising ID.
         Apps that must not request it can remove it with tools:node="remove". -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

</manifest>
